[project]
name = "aws-ai-playground"
version = "0.1.0"
description = "Examples of AWS AI and Machine Learning resources"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.43.34",
    "loguru>=0.7.3",
]
